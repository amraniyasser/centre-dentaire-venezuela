name: Redesign cabinet contact section

on:
  push:
    paths:
      - '.github/scripts/redesign_cabinet_contact.py'
      - '.github/workflows/apply-cabinet-contact.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/scripts/redesign_cabinet_contact.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Replace cabinet perks with contact details" || exit 0
          git push
