name: Apply SEO headings

on:
  push:
    paths:
      - '.github/scripts/seo_headings_patch.py'
      - '.github/workflows/apply-seo-headings.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/scripts/seo_headings_patch.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Improve SEO headings and page title"
          git push
