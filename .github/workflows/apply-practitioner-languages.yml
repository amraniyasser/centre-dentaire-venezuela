name: Add practitioner languages

on:
  push:
    paths:
      - '.github/scripts/add_practitioner_languages.py'
      - '.github/workflows/apply-practitioner-languages.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/scripts/add_practitioner_languages.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Add practitioner languages" || exit 0
          git push
